import React, { memo } from 'react';
// import { Button } from 'react-bootstrap';

const App = memo(() => (
  <div>
    {/* <Button variant="primary">Primary</Button> */}
    測試
  </div>
));

App.propTypes = {};

export default App;
